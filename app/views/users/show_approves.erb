<% provide(:title, @title) %>
<% if current_user?(@user) %>
    <section >
      <ol>
        <% @user_colleges = get_pending_approvals(@user.id) %>
        <%= render :partial => 'shared/pending_approval_item', :collection=> @user_colleges %>
      </ol>
    </section>
<% else %>
    <h1>You think, I am stupid ?? !! </h1>
<% end %>