<%= form_for(@user, :html => { :method => :post }, :url => user_follow_relationship_path(@user), :remote => true) do |f| %>
    <%= f.submit " Follow ", :class => "btn btn-large btn-primary" %>
<% end %>
