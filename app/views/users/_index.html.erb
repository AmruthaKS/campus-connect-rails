<%= will_paginate %>
<ul class="users">
  <% @users.each do |user| %>
    <li>
      <%= image_tag user.avatar.url(:thumb), :size => "50x50" %>
      <%= link_to user.name, user %>
    </li>
  <% end %>
 <%= will_paginate %>
</ul>