<%= will_paginate %>
<ul class="users">
  <% @users.each do |user| %>
    <li>
      <%= image_tag user.avatar.url(:thumb) %>
      <%= link_to user.name, user %>
    </li>
  <% end %>
 <%= will_paginate %>
</ul>