<%= form_for(@user, :html => { :method => :post }, :url => user_unfollow_relationship_path(@user), :remote => true) do |f| %>
    <%= f.hidden_field :referred_id, :value => @referred_id %>
    <%= f.submit " UnFollow ", :class => "btn btn-large" %>
<% end %>